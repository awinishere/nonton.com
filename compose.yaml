services:
  postgres:
    image: postgres:17-alpine
    container_name: pg_nonton.com_dev
    restart: always
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    ports:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: rd_nonton.com_dev
    restart: always
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data: